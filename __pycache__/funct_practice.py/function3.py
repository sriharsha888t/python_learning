# defining a function
def square(self):
    squares = []
    for every in lst:
        squares.append(every ** 2)
    return squares


lst = [1,2,3,4,5]   # declaring the list of values
result = square(lst)    # passing the values into the string
print("The squares of the list is:",result) #printing the result
